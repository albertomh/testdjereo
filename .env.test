DEBUG=false
SECRET_KEY="insecure-test-secret-key"
ALLOWED_HOSTS=127.0.0.1,::1
# CSRF_TRUSTED_ORIGINS entries must consist of scheme and hostname
CSRF_TRUSTED_ORIGINS=http://127.0.0.1
# TODO: if updating the password below, update it in the GitHub action specs as well
DATABASE_URL="postgres://testdjereo:password@localhost:5432/testdjereo"
